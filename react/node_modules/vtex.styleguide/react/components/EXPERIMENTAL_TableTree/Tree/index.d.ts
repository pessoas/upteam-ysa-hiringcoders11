import React from 'react';
import { comparatorCurry, Checkboxes, Tree as TreeType } from '../../EXPERIMENTAL_useCheckboxTree/types';
import { Column, Items } from '../../EXPERIMENTAL_Table/types';
import { Density } from '../../EXPERIMENTAL_Table/hooks/useTableMeasures';
declare type TreeProps = {
    items: Items;
    currentDensity: Density;
    nodesKey: string;
    columns: Array<Column>;
    comparator: comparatorCurry<TreeType<unknown>>;
    rowHeight: number;
    checkboxes?: Checkboxes<unknown>;
    onRowClick?: ({ rowData: unknown }: {
        rowData: any;
    }) => void;
};
declare const _default: React.NamedExoticComponent<TreeProps>;
export default _default;

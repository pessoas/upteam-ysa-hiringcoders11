import { FC } from 'react';
import { RowProps } from './Row';
import { Column, Items } from '../types';
import { Density } from '../hooks/useTableMeasures';
import { Checkboxes } from '../../EXPERIMENTAL_useCheckboxTree/types';
declare const Rows: FC<RowsProps>;
export declare type RowsProps = {
    columns: Array<Column>;
    items: Items;
    currentDensity: Density;
    rowKey?: ({ rowData: unknown }: {
        rowData: any;
    }) => string;
    onRowClick?: ({ rowData: unknown }: {
        rowData: any;
    }) => void;
    isRowActive?: (rowData: unknown) => boolean;
    rowProps?: RowProps;
    rowHeight: number;
    checkboxes?: Checkboxes<unknown>;
    highlightOnHover?: boolean;
};
export default Rows;
